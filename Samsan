--[[
    CYBER PANEL V8 RU
    Компактная версия для Xeno / Roblox.

    Вкладка "ПЕРСОНАЖ":
      • Ник + координаты + копирование
      • Скорость + ползунок
      • Noclip
      • Fly WASD / Space / Ctrl + скорость
      • Автоклик
      • Бесконечный прыжок

    Вкладка "АВТО":
      • Автоперерождение при 100% / максимальном уровне
      • Жёсткий Anti-AFK

    Автоперерождение использует обычную бесплатную кнопку
    "Возрождение" интерфейса игры. Платное AUTO_REBIRTH не используется.

    Все функции при запуске ВЫКЛ.
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local VirtualUser = game:GetService("VirtualUser")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Remotes = ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Remotes")
local RequestAttack = Remotes:WaitForChild("RequestAttack")
local PlayerClick = Remotes:WaitForChild("PlayerClick")
local camera = workspace.CurrentCamera
local playerGui = player:WaitForChild("PlayerGui")

local state = {
    speed = false,
    speedValue = 50,

    noclip = false,

    fly = false,
    flySpeed = 80,

    autoClick = false,
    clickInterval = 0.08,

    infiniteJump = false,

    autoRebirth = false,
    antiAfk = false,
}

local alive = true
local connections = {}
local originalCollisions = {}
local originalWalkSpeed = nil
local autoStatusLabel = nil

local function addConnection(c)
    table.insert(connections, c)
    return c
end

local function getCharacter()
    return player.Character
end

local function getHumanoid()
    local char = getCharacter()
    return char and char:FindFirstChildOfClass("Humanoid")
end

local function getRoot()
    local char = getCharacter()
    return char and char:FindFirstChild("HumanoidRootPart")
end

local function setAutoStatus(text)
    if autoStatusLabel and autoStatusLabel.Parent then
        autoStatusLabel.Text = tostring(text)
    end
end

-- =========================================================
-- GUI PARENT / CLEANUP
-- =========================================================

local guiParent = CoreGui

if type(gethui) == "function" then
    local ok, hui = pcall(gethui)
    if ok and hui then
        guiParent = hui
    end
end

for _, name in ipairs({
    "CyberPanel",
    "CyberPanelV1",
    "CyberPanelV5",
    "CyberPanelV6",
    "CyberPanelV7",
    "CyberPanelV8",
}) do
    local old = guiParent:FindFirstChild(name)
    if old then
        old:Destroy()
    end
end

-- =========================================================
-- COLORS
-- =========================================================

local C = {
    bg = Color3.fromRGB(7, 12, 17),
    panel = Color3.fromRGB(11, 18, 24),
    panel2 = Color3.fromRGB(15, 24, 31),

    cyan = Color3.fromRGB(44, 214, 255),
    cyanDim = Color3.fromRGB(27, 84, 101),

    text = Color3.fromRGB(230, 248, 255),
    muted = Color3.fromRGB(125, 157, 169),

    green = Color3.fromRGB(52, 222, 139),
    off = Color3.fromRGB(48, 59, 67),
    red = Color3.fromRGB(224, 73, 86),
}

local function round(obj, radius)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, radius or 7)
    c.Parent = obj
end

local function addStroke(obj, color, trans)
    local s = Instance.new("UIStroke")
    s.Color = color or C.cyanDim
    s.Transparency = trans or 0.55
    s.Thickness = 1
    s.Parent = obj
end

local function makeLabel(parent, text, pos, size, textSize, color, font)
    local l = Instance.new("TextLabel")
    l.BackgroundTransparency = 1
    l.Position = pos
    l.Size = size
    l.Text = text
    l.TextColor3 = color or C.text
    l.TextSize = textSize or 10
    l.Font = font or Enum.Font.GothamMedium
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.TextYAlignment = Enum.TextYAlignment.Center
    l.Parent = parent
    return l
end

local function makeButton(parent, text, pos, size)
    local b = Instance.new("TextButton")
    b.Position = pos
    b.Size = size
    b.BackgroundColor3 = C.panel2
    b.BackgroundTransparency = 0.03
    b.BorderSizePixel = 0
    b.AutoButtonColor = false
    b.Text = text
    b.TextColor3 = C.text
    b.TextSize = 9
    b.Font = Enum.Font.GothamBold
    b.Parent = parent

    round(b, 6)
    addStroke(b, C.cyanDim, 0.58)

    return b
end

-- =========================================================
-- ROOT GUI
-- =========================================================

local gui = Instance.new("ScreenGui")
gui.Name = "CyberPanelV8"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = guiParent

local main = Instance.new("Frame")
main.Name = "Main"
main.Size = UDim2.fromOffset(330, 500)
main.Position = UDim2.new(0, 18, 0.5, -250)
main.BackgroundColor3 = C.bg
main.BackgroundTransparency = 0.10
main.BorderSizePixel = 0
main.Active = true
main.Parent = gui
round(main, 10)
addStroke(main, C.cyan, 0.28)

local topLine = Instance.new("Frame")
topLine.Size = UDim2.new(1, -20, 0, 2)
topLine.Position = UDim2.fromOffset(10, 0)
topLine.BackgroundColor3 = C.cyan
topLine.BorderSizePixel = 0
topLine.Parent = main

-- =========================================================
-- HEADER
-- =========================================================

makeLabel(
    main,
    "CYBER // PANEL",
    UDim2.fromOffset(12, 7),
    UDim2.fromOffset(190, 19),
    11,
    C.text,
    Enum.Font.GothamBold
)

local closeBtn = makeButton(
    main,
    "×",
    UDim2.new(1, -34, 0, 6),
    UDim2.fromOffset(24, 24)
)
closeBtn.TextSize = 15
closeBtn.BackgroundColor3 = C.red
closeBtn.BackgroundTransparency = 0.15

local playerLabel = makeLabel(
    main,
    "ИГРОК // " .. player.Name,
    UDim2.fromOffset(12, 28),
    UDim2.new(1, -24, 0, 18),
    9,
    C.cyan,
    Enum.Font.GothamBold
)

local coordsBox = Instance.new("Frame")
coordsBox.Position = UDim2.fromOffset(10, 50)
coordsBox.Size = UDim2.new(1, -20, 0, 36)
coordsBox.BackgroundColor3 = C.panel
coordsBox.BackgroundTransparency = 0.08
coordsBox.BorderSizePixel = 0
coordsBox.Parent = main
round(coordsBox, 7)
addStroke(coordsBox, C.cyanDim, 0.58)

local coordsLabel = makeLabel(
    coordsBox,
    "X 0.00   Y 0.00   Z 0.00",
    UDim2.fromOffset(9, 0),
    UDim2.new(1, -77, 1, 0),
    8,
    C.text,
    Enum.Font.Code
)

local copyBtn = makeButton(
    coordsBox,
    "КОПИЯ",
    UDim2.new(1, -66, 0.5, -12),
    UDim2.fromOffset(58, 24)
)

-- =========================================================
-- TABS
-- =========================================================

local tabs = Instance.new("Frame")
tabs.Position = UDim2.fromOffset(10, 92)
tabs.Size = UDim2.new(1, -20, 0, 28)
tabs.BackgroundTransparency = 1
tabs.Parent = main

local charTab = makeButton(
    tabs,
    "ПЕРСОНАЖ",
    UDim2.fromOffset(0, 0),
    UDim2.new(0.5, -3, 1, 0)
)

local autoTab = makeButton(
    tabs,
    "АВТО",
    UDim2.new(0.5, 3, 0, 0),
    UDim2.new(0.5, -3, 1, 0)
)

local charPage = Instance.new("Frame")
charPage.Position = UDim2.fromOffset(10, 126)
charPage.Size = UDim2.new(1, -20, 1, -136)
charPage.BackgroundTransparency = 1
charPage.Parent = main

local autoPage = Instance.new("Frame")
autoPage.Position = charPage.Position
autoPage.Size = charPage.Size
autoPage.BackgroundTransparency = 1
autoPage.Visible = false
autoPage.Parent = main

local function setTab(name)
    local charActive = name == "CHAR"

    charPage.Visible = charActive
    autoPage.Visible = not charActive

    charTab.BackgroundColor3 = charActive and C.cyan or C.panel2
    charTab.TextColor3 = charActive and C.bg or C.text

    autoTab.BackgroundColor3 = not charActive and C.cyan or C.panel2
    autoTab.TextColor3 = not charActive and C.bg or C.text
end

setTab("CHAR")

addConnection(charTab.MouseButton1Click:Connect(function()
    setTab("CHAR")
end))

addConnection(autoTab.MouseButton1Click:Connect(function()
    setTab("AUTO")
end))

-- =========================================================
-- ROW / TOGGLE
-- =========================================================

local function makeRow(parent, y, text, height)
    local row = Instance.new("Frame")
    row.Position = UDim2.fromOffset(0, y)
    row.Size = UDim2.new(1, 0, 0, height or 34)
    row.BackgroundColor3 = C.panel
    row.BackgroundTransparency = 0.08
    row.BorderSizePixel = 0
    row.Parent = parent

    round(row, 7)
    addStroke(row, C.cyanDim, 0.65)

    makeLabel(
        row,
        text,
        UDim2.fromOffset(9, 0),
        UDim2.new(1, -73, 1, 0),
        8,
        C.text,
        Enum.Font.GothamMedium
    )

    return row
end

local function makeToggle(row, initial, callback)
    local current = initial

    local b = makeButton(
        row,
        "",
        UDim2.new(1, -58, 0.5, -11),
        UDim2.fromOffset(49, 22)
    )

    local function redraw()
        if current then
            b.BackgroundColor3 = C.green
            b.TextColor3 = C.bg
            b.Text = "ВКЛ"
        else
            b.BackgroundColor3 = C.off
            b.TextColor3 = C.text
            b.Text = "ВЫКЛ"
        end
    end

    addConnection(b.MouseButton1Click:Connect(function()
        current = not current
        redraw()
        callback(current)
    end))

    redraw()
    return b
end

local function makeCompactSlider(parent, y, titleText, minValue, maxValue, initialValue, callback)
    local box = Instance.new("Frame")
    box.Position = UDim2.fromOffset(0, y)
    box.Size = UDim2.new(1, 0, 0, 43)
    box.BackgroundColor3 = C.panel
    box.BackgroundTransparency = 0.08
    box.BorderSizePixel = 0
    box.Parent = parent

    round(box, 7)
    addStroke(box, C.cyanDim, 0.65)

    makeLabel(
        box,
        titleText,
        UDim2.fromOffset(9, 2),
        UDim2.new(1, -55, 0, 17),
        7,
        C.muted,
        Enum.Font.GothamBold
    )

    local valueLabel = makeLabel(
        box,
        tostring(initialValue),
        UDim2.new(1, -47, 0, 2),
        UDim2.fromOffset(38, 17),
        8,
        C.cyan,
        Enum.Font.Code
    )
    valueLabel.TextXAlignment = Enum.TextXAlignment.Right

    local track = Instance.new("Frame")
    track.Position = UDim2.fromOffset(9, 28)
    track.Size = UDim2.new(1, -18, 0, 4)
    track.BackgroundColor3 = C.off
    track.BorderSizePixel = 0
    track.Parent = box
    round(track, 2)

    local fill = Instance.new("Frame")
    fill.BackgroundColor3 = C.cyan
    fill.BorderSizePixel = 0
    fill.Parent = track
    round(fill, 2)

    local knob = Instance.new("Frame")
    knob.AnchorPoint = Vector2.new(0.5, 0.5)
    knob.Size = UDim2.fromOffset(9, 9)
    knob.BackgroundColor3 = C.text
    knob.BorderSizePixel = 0
    knob.Parent = track
    round(knob, 5)

    local hit = Instance.new("TextButton")
    hit.BackgroundTransparency = 1
    hit.Text = ""
    hit.Position = UDim2.fromOffset(-3, -8)
    hit.Size = UDim2.new(1, 6, 0, 20)
    hit.Parent = track

    local dragging = false

    local function setValue(v)
        v = math.floor(math.clamp(v, minValue, maxValue) + 0.5)

        local alpha =
            (v - minValue) /
            math.max(maxValue - minValue, 1)

        fill.Size = UDim2.new(alpha, 0, 1, 0)
        knob.Position = UDim2.new(alpha, 0, 0.5, 0)
        valueLabel.Text = tostring(v)

        callback(v)
    end

    local function setFromX(x)
        local alpha = math.clamp(
            (x - track.AbsolutePosition.X) /
            math.max(track.AbsoluteSize.X, 1),
            0,
            1
        )

        setValue(
            minValue +
            (maxValue - minValue) * alpha
        )
    end

    addConnection(hit.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch
        then
            dragging = true
            setFromX(input.Position.X)
        end
    end))

    addConnection(UserInputService.InputChanged:Connect(function(input)
        if dragging and (
            input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch
        ) then
            setFromX(input.Position.X)
        end
    end))

    addConnection(UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch
        then
            dragging = false
        end
    end))

    setValue(initialValue)
end

-- =========================================================
-- CHARACTER TAB
-- =========================================================

local speedRow = makeRow(charPage, 0, "СКОРОСТЬ ИГРОКА")
makeToggle(speedRow, false, function(v)
    state.speed = v

    local humanoid = getHumanoid()
    if not humanoid then return end

    if v then
        if originalWalkSpeed == nil then
            originalWalkSpeed = humanoid.WalkSpeed
        end

        humanoid.WalkSpeed = state.speedValue
    else
        if originalWalkSpeed ~= nil then
            humanoid.WalkSpeed = originalWalkSpeed
        end
    end
end)

makeCompactSlider(
    charPage,
    39,
    "СКОРОСТЬ",
    16,
    250,
    state.speedValue,
    function(v)
        state.speedValue = v

        if state.speed then
            local humanoid = getHumanoid()
            if humanoid then
                humanoid.WalkSpeed = v
            end
        end
    end
)

local noclipRow = makeRow(charPage, 87, "ПРОХОД ЧЕРЕЗ СТЕНЫ")
makeToggle(noclipRow, false, function(v)
    state.noclip = v

    if not v then
        for part, oldValue in pairs(originalCollisions) do
            if part and part.Parent then
                pcall(function()
                    part.CanCollide = oldValue
                end)
            end
        end

        table.clear(originalCollisions)
    end
end)

local flyRow = makeRow(charPage, 126, "ПОЛЁТ WASD + SPACE / CTRL")
makeToggle(flyRow, false, function(v)
    state.fly = v

    local humanoid = getHumanoid()
    local root = getRoot()

    if humanoid then
        humanoid.PlatformStand = v
    end

    if not v and root then
        root.AssemblyLinearVelocity = Vector3.zero
        root.AssemblyAngularVelocity = Vector3.zero
    end
end)

makeCompactSlider(
    charPage,
    165,
    "СКОРОСТЬ ПОЛЁТА",
    10,
    300,
    state.flySpeed,
    function(v)
        state.flySpeed = v
    end
)

local clickRow = makeRow(charPage, 213, "АВТОКЛИК ЛКМ")
makeToggle(clickRow, false, function(v)
    state.autoClick = v
end)

local jumpRow = makeRow(charPage, 252, "БЕСКОНЕЧНЫЙ ПРЫЖОК")
makeToggle(jumpRow, false, function(v)
    state.infiniteJump = v
end)

-- =========================================================
-- TELEPORT BY COORDINATES
-- Формат: -1211.58, 4.19, -895.05
-- =========================================================

local tpBox = Instance.new("Frame")
tpBox.Position = UDim2.fromOffset(0, 291)
tpBox.Size = UDim2.new(1, 0, 0, 64)
tpBox.BackgroundColor3 = C.panel
tpBox.BackgroundTransparency = 0.08
tpBox.BorderSizePixel = 0
tpBox.Parent = charPage
round(tpBox, 7)
addStroke(tpBox, C.cyanDim, 0.65)

makeLabel(
    tpBox,
    "ТЕЛЕПОРТ ПО КООРДИНАТАМ",
    UDim2.fromOffset(9, 2),
    UDim2.new(1, -18, 0, 18),
    7,
    C.muted,
    Enum.Font.GothamBold
)

local tpInput = Instance.new("TextBox")
tpInput.Position = UDim2.fromOffset(9, 24)
tpInput.Size = UDim2.new(1, -76, 0, 31)
tpInput.BackgroundColor3 = C.panel2
tpInput.BackgroundTransparency = 0.02
tpInput.BorderSizePixel = 0
tpInput.ClearTextOnFocus = false
tpInput.PlaceholderText = "-1211.58, 4.19, -895.05"
tpInput.Text = ""
tpInput.TextColor3 = C.text
tpInput.PlaceholderColor3 = C.muted
tpInput.TextSize = 8
tpInput.Font = Enum.Font.Code
tpInput.TextXAlignment = Enum.TextXAlignment.Left
tpInput.Parent = tpBox
round(tpInput, 6)
addStroke(tpInput, C.cyanDim, 0.62)

local tpButton = makeButton(
    tpBox,
    "ТП",
    UDim2.new(1, -60, 0, 24),
    UDim2.fromOffset(51, 31)
)
tpButton.BackgroundColor3 = C.cyan
tpButton.TextColor3 = C.bg

local function parseTeleportCoordinates(raw)
    local nums = {}

    for token in string.gmatch(
        tostring(raw or ""),
        "[-+]?%d*%.?%d+"
    ) do
        local n = tonumber(token)

        if n then
            nums[#nums + 1] = n

            if #nums >= 3 then
                break
            end
        end
    end

    if #nums < 3 then
        return nil
    end

    return nums[1], nums[2], nums[3]
end

local function teleportFromInput()
    local x, y, z = parseTeleportCoordinates(tpInput.Text)

    if not x then
        tpInput.Text = ""
        tpInput.PlaceholderText = "ОШИБКА: X, Y, Z"
        return
    end

    local root = getRoot()

    if not root then
        tpInput.PlaceholderText = "ПЕРСОНАЖ НЕ НАЙДЕН"
        return
    end

    pcall(function()
        root.AssemblyLinearVelocity = Vector3.zero
        root.AssemblyAngularVelocity = Vector3.zero
        root.CFrame = CFrame.new(x, y, z)
    end)

    tpInput.Text = string.format(
        "%.2f, %.2f, %.2f",
        x,
        y,
        z
    )
end

addConnection(
    tpButton.MouseButton1Click:Connect(
        teleportFromInput
    )
)

addConnection(
    tpInput.FocusLost:Connect(function(enterPressed)
        if enterPressed then
            teleportFromInput()
        end
    end)
)

-- =========================================================
-- CLICK / ATTACK ENGINE
-- =========================================================

-- Из декомпиляции AttackController:
-- RequestAttack:FireServer()
-- PlayerClick:FireServer()
-- task.wait(0.05)
--
-- То есть для настоящей атаки серверу НЕ нужны enemyId/position.
-- Сервер сам выбирает цель.

local function performServerAttack()
    local ok = pcall(function()
        RequestAttack:FireServer()
        PlayerClick:FireServer()
    end)

    return ok
end

-- Локальные fallbacks оставляем только как резерв.
local function performLocalClickFallback()
    local didSomething = false

    if type(mouse1click) == "function" then
        local ok = pcall(mouse1click)
        didSomething = didSomething or ok
    elseif type(mouse1press) == "function"
        and type(mouse1release) == "function"
    then
        local ok = pcall(function()
            mouse1press()
            task.wait(0.015)
            mouse1release()
        end)
        didSomething = didSomething or ok
    end

    local char = getCharacter()
    local tool = char and char:FindFirstChildOfClass("Tool")

    if tool then
        local ok = pcall(function()
            tool:Activate()
        end)
        didSomething = didSomething or ok
    end

    if not didSomething then
        local mouse = UserInputService:GetMouseLocation()

        pcall(function()
            VirtualUser:Button1Down(
                Vector2.new(mouse.X, mouse.Y),
                camera and camera.CFrame or CFrame.new()
            )

            task.wait(0.015)

            VirtualUser:Button1Up(
                Vector2.new(mouse.X, mouse.Y),
                camera and camera.CFrame or CFrame.new()
            )
        end)
    end
end

task.spawn(function()
    while alive do
        if state.autoClick then
            local ok = performServerAttack()

            if not ok then
                performLocalClickFallback()
            end

            task.wait(0.05)
        else
            task.wait(0.06)
        end
    end
end)

-- =========================================================
-- AUTO TAB // AUTO REBIRTH + HARD ANTI-AFK
-- =========================================================

local autoInfo = Instance.new("Frame")
autoInfo.Position = UDim2.fromOffset(0, 0)
autoInfo.Size = UDim2.new(1, 0, 0, 58)
autoInfo.BackgroundColor3 = C.panel
autoInfo.BackgroundTransparency = 0.08
autoInfo.BorderSizePixel = 0
autoInfo.Parent = autoPage
round(autoInfo, 7)
addStroke(autoInfo, C.cyanDim, 0.65)

makeLabel(
    autoInfo,
    "АВТОМАТИЗАЦИЯ",
    UDim2.fromOffset(9, 3),
    UDim2.new(1, -18, 0, 18),
    9,
    C.cyan,
    Enum.Font.GothamBold
)

makeLabel(
    autoInfo,
    "REBIRTH 100% // HARD ANTI-AFK",
    UDim2.fromOffset(9, 25),
    UDim2.new(1, -18, 0, 20),
    7,
    C.muted,
    Enum.Font.Code
)

local restoreIdleConnections = function() end

local rebirthRow = makeRow(
    autoPage,
    66,
    "АВТОПЕРЕРОЖДЕНИЕ 100%"
)

makeToggle(rebirthRow, false, function(v)
    state.autoRebirth = v

    if v then
        setAutoStatus("Автоперерождение ВКЛ // ждём 100%")
    else
        setAutoStatus("Автоперерождение выключено")
    end
end)

local antiAfkRow = makeRow(
    autoPage,
    106,
    "ANTI-AFK // ЖЁСТКИЙ"
)

makeToggle(antiAfkRow, false, function(v)
    state.antiAfk = v

    if v then
        setAutoStatus("Anti-AFK ВКЛ // защита активна")
    else
        restoreIdleConnections()
        setAutoStatus("Anti-AFK выключен")
    end
end)

autoStatusLabel = makeLabel(
    autoPage,
    "Все функции выключены",
    UDim2.fromOffset(4, 152),
    UDim2.new(1, -8, 0, 58),
    8,
    C.muted,
    Enum.Font.Code
)
autoStatusLabel.TextWrapped = true
autoStatusLabel.TextYAlignment = Enum.TextYAlignment.Top

makeLabel(
    autoPage,
    "Автоперерождение нажимает только обычную бесплатную кнопку «Возрождение». Платный AUTO_REBIRTH не используется.",
    UDim2.fromOffset(4, 220),
    UDim2.new(1, -8, 0, 58),
    7,
    C.muted,
    Enum.Font.Code
).TextWrapped = true

makeLabel(
    autoPage,
    "Anti-AFK: Idled + VirtualUser + mouse pulse + tiny movement. Не защищает от ручного кика админом или разрыва соединения.",
    UDim2.fromOffset(4, 286),
    UDim2.new(1, -8, 0, 62),
    7,
    C.muted,
    Enum.Font.Code
).TextWrapped = true

-- =========================================================
-- GUI HELPERS FOR AUTO REBIRTH
-- =========================================================

local function normalizeGuiText(value)
    local s = string.lower(tostring(value or ""))
    s = s:gsub("%s+", " ")
    return s
end

local function isGuiVisible(obj)
    if not obj or not obj:IsA("GuiObject") then
        return false
    end

    if obj.AbsoluteSize.X < 2 or obj.AbsoluteSize.Y < 2 then
        return false
    end

    local current = obj

    while current and current ~= playerGui do
        if current:IsA("GuiObject") and not current.Visible then
            return false
        end

        if current:IsA("LayerCollector") and not current.Enabled then
            return false
        end

        current = current.Parent
    end

    return true
end

local function getButtonText(button)
    local parts = {}

    if button:IsA("TextButton") then
        parts[#parts + 1] = button.Text
    end

    parts[#parts + 1] = button.Name

    for _, obj in ipairs(button:GetDescendants()) do
        if obj:IsA("TextLabel") or obj:IsA("TextButton") then
            parts[#parts + 1] = obj.Text
        end
    end

    return normalizeGuiText(table.concat(parts, " "))
end

local function parseInteger(text)
    local digits = tostring(text or ""):gsub("%D", "")

    if digits == "" then
        return nil
    end

    return tonumber(digits)
end

local function getLevelReadyState()
    local foundCurrent = nil
    local foundMax = nil

    for _, obj in ipairs(playerGui:GetDescendants()) do
        if (obj:IsA("TextLabel") or obj:IsA("TextButton"))
            and isGuiVisible(obj)
        then
            local raw = tostring(obj.Text or "")
            local low = normalizeGuiText(raw)

            -- Прямой индикатор "МАКСИМАЛЬНЫЙ УРОВЕНЬ".
            if string.find(low, "максимальн", 1, true)
                or string.find(low, "max level", 1, true)
                or string.find(low, "maximum level", 1, true)
            then
                return true, nil, nil, "MAX LEVEL"
            end

            -- Формат из окна: "Уровень 15650 / 15650".
            if string.find(low, "уров", 1, true)
                or string.find(low, "level", 1, true)
            then
                local left, right =
                    raw:match("([%d%s,%.]+)%s*/%s*([%d%s,%.]+)")

                if left and right then
                    local current = parseInteger(left)
                    local maxValue = parseInteger(right)

                    if current and maxValue and maxValue > 0 then
                        foundCurrent = current
                        foundMax = maxValue

                        if current >= maxValue then
                            return true, current, maxValue, "LEVEL RATIO"
                        end
                    end
                end
            end
        end
    end

    return false, foundCurrent, foundMax, "WAIT"
end

local function findRebirthButton(confirmOnly)
    local best = nil
    local bestScore = -1

    for _, obj in ipairs(playerGui:GetDescendants()) do
        if (obj:IsA("TextButton") or obj:IsA("ImageButton"))
            and isGuiVisible(obj)
        then
            local text = getButtonText(obj)

            local isSkip =
                string.find(text, "пропуст", 1, true)
                or string.find(text, "skip", 1, true)

            if not isSkip then
                local score = 0

                if string.find(text, "возрожд", 1, true) then
                    score += 100
                end

                if string.find(text, "rebirth", 1, true) then
                    score += 90
                end

                if not confirmOnly
                    and string.find(text, "перерожд", 1, true)
                then
                    score += 50
                end

                -- Название GUI-объекта тоже учитываем.
                local name = normalizeGuiText(obj.Name)

                if string.find(name, "rebirth", 1, true)
                    or string.find(name, "prestige", 1, true)
                then
                    score += 45
                end

                if confirmOnly
                    and score < 90
                then
                    score = 0
                end

                if score > bestScore and score > 0 then
                    best = obj
                    bestScore = score
                end
            end
        end
    end

    return best, bestScore
end

local function activateGuiButton(button)
    if not button or not button.Parent then
        return false, "button missing"
    end

    -- Xeno поддерживает getconnections. Сначала используем
    -- реальный клиентский callback кнопки, если он доступен.
    if type(getconnections) == "function" then
        for _, signal in ipairs({
            button.Activated,
            button.MouseButton1Click,
        }) do
            local ok, list = pcall(getconnections, signal)

            if ok and type(list) == "table" and #list > 0 then
                local fired = false

                for _, conn in ipairs(list) do
                    local done = false

                    if type(conn.Fire) == "function" then
                        done = pcall(function()
                            conn:Fire()
                        end)
                    elseif type(conn.Function) == "function" then
                        done = pcall(conn.Function)
                    end

                    fired = fired or done
                end

                if fired then
                    return true, "getconnections"
                end
            end
        end
    end

    -- Fallback: настоящий виртуальный клик в центр кнопки.
    local pos = button.AbsolutePosition
    local size = button.AbsoluteSize

    local x = math.floor(pos.X + size.X / 2)
    local y = math.floor(pos.Y + size.Y / 2)

    local ok = pcall(function()
        VirtualInputManager:SendMouseMoveEvent(
            x,
            y,
            game
        )

        task.wait(0.04)

        VirtualInputManager:SendMouseButtonEvent(
            x,
            y,
            0,
            true,
            game,
            0
        )

        task.wait(0.05)

        VirtualInputManager:SendMouseButtonEvent(
            x,
            y,
            0,
            false,
            game,
            0
        )
    end)

    return ok, "virtual click"
end

local lastRebirthAttempt = 0
local rebirthBusy = false

local function tryAutoRebirth()
    if rebirthBusy or not state.autoRebirth then
        return
    end

    local ready, current, maxValue, source =
        getLevelReadyState()

    if not ready then
        if current and maxValue then
            setAutoStatus(
                string.format(
                    "Уровень %d / %d // ждём 100%%",
                    current,
                    maxValue
                )
            )
        else
            setAutoStatus(
                state.antiAfk
                and "Ждём 100% // Anti-AFK активен"
                or "Ждём 100%"
            )
        end

        return
    end

    if os.clock() - lastRebirthAttempt < 2.0 then
        return
    end

    rebirthBusy = true
    lastRebirthAttempt = os.clock()

    task.spawn(function()
        setAutoStatus(
            "100% ДОСТИГНУТО // ищем «Возрождение»..."
        )

        -- Сначала ищем именно кнопку подтверждения.
        local confirm = findRebirthButton(true)

        if confirm then
            local ok, method = activateGuiButton(confirm)

            setAutoStatus(
                ok
                and ("ПЕРЕРОЖДЕНИЕ ✓ // " .. tostring(method))
                or "Не удалось нажать кнопку"
            )

            task.wait(1.5)
            rebirthBusy = false
            return
        end

        -- Если окно ещё закрыто — пытаемся открыть его
        -- через обычную кнопку/иконку Rebirth в GUI.
        local opener = findRebirthButton(false)

        if opener then
            local opened = activateGuiButton(opener)

            if opened then
                setAutoStatus(
                    "100% // открываем окно перерождения..."
                )

                task.wait(0.45)

                local confirm2 = findRebirthButton(true)

                if confirm2 then
                    local ok2, method2 =
                        activateGuiButton(confirm2)

                    setAutoStatus(
                        ok2
                        and ("ПЕРЕРОЖДЕНИЕ ✓ // " .. tostring(method2))
                        or "Окно открыто, но клик не прошёл"
                    )
                else
                    setAutoStatus(
                        "100% // окно открыто, кнопка не найдена"
                    )
                end
            else
                setAutoStatus(
                    "100% // не удалось открыть окно"
                )
            end
        else
            setAutoStatus(
                "100% // кнопка Rebirth не найдена в GUI"
            )
        end

        task.wait(1.5)
        rebirthBusy = false
    end)
end

task.spawn(function()
    while alive do
        if state.autoRebirth then
            pcall(tryAutoRebirth)
            task.wait(0.25)
        else
            task.wait(0.50)
        end
    end
end)

-- =========================================================
-- HARD ANTI-AFK
-- =========================================================

local antiAfkPulseCount = 0
local disabledIdleConnections = {}

local function disableIdleConnections()
    if type(getconnections) ~= "function" then
        return
    end

    local ok, list = pcall(
        getconnections,
        player.Idled
    )

    if not ok or type(list) ~= "table" then
        return
    end

    for _, conn in ipairs(list) do
        pcall(function()
            if type(conn.Disable) == "function"
                and type(conn.Enable) == "function"
                and not disabledIdleConnections[conn]
            then
                conn:Disable()
                disabledIdleConnections[conn] = true
            end
        end)
    end
end

restoreIdleConnections = function()
    for conn in pairs(disabledIdleConnections) do
        pcall(function()
            if type(conn.Enable) == "function" then
                conn:Enable()
            end
        end)

        disabledIdleConnections[conn] = nil
    end
end

local function antiAfkPulse()
    if not state.antiAfk then
        return
    end

    antiAfkPulseCount += 1

    -- 1) VirtualUser — классический keep-alive.
    pcall(function()
        VirtualUser:CaptureController()

        VirtualUser:Button2Down(
            Vector2.new(0, 0),
            camera and camera.CFrame or CFrame.new()
        )

        task.wait(0.04)

        VirtualUser:Button2Up(
            Vector2.new(0, 0),
            camera and camera.CFrame or CFrame.new()
        )
    end)

    -- 2) Минимальное движение мыши: создаёт реальный input pulse,
    -- но почти не мешает игре.
    pcall(function()
        local mouse = UserInputService:GetMouseLocation()

        VirtualInputManager:SendMouseMoveEvent(
            math.floor(mouse.X + 1),
            math.floor(mouse.Y),
            game
        )

        task.wait(0.03)

        VirtualInputManager:SendMouseMoveEvent(
            math.floor(mouse.X),
            math.floor(mouse.Y),
            game
        )
    end)

    -- 3) Tiny movement для игр с собственным AFK-контролем.
    -- Позиция возвращается обратно, поэтому игрок не уходит с места.
    local root = getRoot()

    if root and not state.fly then
        pcall(function()
            local original = root.CFrame

            root.CFrame =
                original *
                CFrame.new(0.025, 0, 0)

            task.wait(0.035)

            if root and root.Parent then
                root.CFrame = original
            end
        end)
    end

    disableIdleConnections()

    if state.autoRebirth then
        setAutoStatus(
            "AUTO REBIRTH + ANTI-AFK // pulse #" ..
            tostring(antiAfkPulseCount)
        )
    else
        setAutoStatus(
            "ANTI-AFK АКТИВЕН // pulse #" ..
            tostring(antiAfkPulseCount)
        )
    end
end

-- На сам Roblox Idled реагируем немедленно.
addConnection(player.Idled:Connect(function()
    if state.antiAfk then
        task.spawn(antiAfkPulse)
    end
end))

-- И дополнительно даём активность заранее,
-- не дожидаясь стандартного idle timeout.
task.spawn(function()
    while alive do
        if state.antiAfk then
            antiAfkPulse()
            task.wait(22)
        else
            task.wait(1)
        end
    end
end)

-- =========================================================
-- COORDINATES
-- =========================================================

local function coordinateStrings()
    local root = getRoot()

    if not root then
        return "X 0.00   Y 0.00   Z 0.00",
               "0.00, 0.00, 0.00"
    end

    local p = root.Position

    return string.format(
        "X %.2f   Y %.2f   Z %.2f",
        p.X,
        p.Y,
        p.Z
    ),
    string.format(
        "%.2f, %.2f, %.2f",
        p.X,
        p.Y,
        p.Z
    )
end

addConnection(copyBtn.MouseButton1Click:Connect(function()
    local _, raw = coordinateStrings()

    local copied = false

    if type(setclipboard) == "function" then
        copied = pcall(setclipboard, raw)
    elseif type(toclipboard) == "function" then
        copied = pcall(toclipboard, raw)
    end

    if not copied then
        print("[CYBER PANEL] COORDS:", raw)
    end

    copyBtn.Text = copied and "ГОТОВО" or "В КОНСОЛИ"

    task.delay(0.7, function()
        if copyBtn and copyBtn.Parent then
            copyBtn.Text = "КОПИЯ"
        end
    end)
end))

-- =========================================================
-- NOCLIP
-- =========================================================

addConnection(RunService.Stepped:Connect(function()
    if not alive or not state.noclip then
        return
    end

    local char = getCharacter()
    if not char then
        return
    end

    for _, obj in ipairs(char:GetDescendants()) do
        if obj:IsA("BasePart") then
            if originalCollisions[obj] == nil then
                originalCollisions[obj] = obj.CanCollide
            end

            obj.CanCollide = false
        end
    end
end))

-- =========================================================
-- SPEED ENFORCER
-- =========================================================

addConnection(RunService.Heartbeat:Connect(function()
    if not alive or not state.speed then
        return
    end

    local humanoid = getHumanoid()

    if humanoid
        and humanoid.WalkSpeed ~= state.speedValue
    then
        humanoid.WalkSpeed = state.speedValue
    end
end))

-- =========================================================
-- FLY
-- =========================================================

local flyKeys = {
    W = false,
    A = false,
    S = false,
    D = false,
    UP = false,
    DOWN = false,
}

local keyMap = {
    [Enum.KeyCode.W] = "W",
    [Enum.KeyCode.A] = "A",
    [Enum.KeyCode.S] = "S",
    [Enum.KeyCode.D] = "D",
    [Enum.KeyCode.Space] = "UP",
    [Enum.KeyCode.LeftControl] = "DOWN",
    [Enum.KeyCode.RightControl] = "DOWN",
}

addConnection(UserInputService.InputBegan:Connect(function(input, processed)
    if processed then
        return
    end

    local key = keyMap[input.KeyCode]

    if key then
        flyKeys[key] = true
    end
end))

addConnection(UserInputService.InputEnded:Connect(function(input)
    local key = keyMap[input.KeyCode]

    if key then
        flyKeys[key] = false
    end
end))

addConnection(RunService.RenderStepped:Connect(function()
    if not alive then
        return
    end

    local display = coordinateStrings()
    coordsLabel.Text = display

    if not state.fly then
        return
    end

    camera = workspace.CurrentCamera or camera

    local root = getRoot()
    local humanoid = getHumanoid()

    if not root or not humanoid or not camera then
        return
    end

    humanoid.PlatformStand = true

    local direction = Vector3.zero
    local cf = camera.CFrame

    if flyKeys.W then
        direction += cf.LookVector
    end

    if flyKeys.S then
        direction -= cf.LookVector
    end

    if flyKeys.D then
        direction += cf.RightVector
    end

    if flyKeys.A then
        direction -= cf.RightVector
    end

    if flyKeys.UP then
        direction += Vector3.yAxis
    end

    if flyKeys.DOWN then
        direction -= Vector3.yAxis
    end

    if direction.Magnitude > 0 then
        direction = direction.Unit
    end

    root.AssemblyLinearVelocity =
        direction * state.flySpeed

    root.AssemblyAngularVelocity = Vector3.zero
end))

-- =========================================================
-- INFINITE JUMP
-- =========================================================

addConnection(UserInputService.JumpRequest:Connect(function()
    if not alive or not state.infiniteJump then
        return
    end

    local humanoid = getHumanoid()

    if humanoid then
        humanoid:ChangeState(
            Enum.HumanoidStateType.Jumping
        )
    end
end))

-- =========================================================
-- RESPAWN
-- =========================================================

addConnection(player.CharacterAdded:Connect(function(char)
    originalWalkSpeed = nil
    table.clear(originalCollisions)

    local humanoid =
        char:WaitForChild("Humanoid", 10)

    task.wait(0.15)

    if state.speed and humanoid then
        originalWalkSpeed = humanoid.WalkSpeed
        humanoid.WalkSpeed = state.speedValue
    end

    if state.fly and humanoid then
        humanoid.PlatformStand = true
    end
end))

-- =========================================================
-- DRAG HEADER
-- =========================================================

do
    local dragging = false
    local dragStart = nil
    local startPos = nil

    addConnection(main.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch
        then
            local localY =
                input.Position.Y -
                main.AbsolutePosition.Y

            if localY <= 48 then
                dragging = true
                dragStart = input.Position
                startPos = main.Position
            end
        end
    end))

    addConnection(UserInputService.InputChanged:Connect(function(input)
        if dragging and (
            input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch
        ) then
            local delta =
                input.Position - dragStart

            main.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end))

    addConnection(UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch
        then
            dragging = false
        end
    end))
end

-- =========================================================
-- SHUTDOWN
-- =========================================================

local function shutdown()
    if not alive then
        return
    end

    alive = false

    state.speed = false
    state.noclip = false
    state.fly = false
    state.autoClick = false
    state.infiniteJump = false
    state.autoRebirth = false
    state.antiAfk = false
    restoreIdleConnections()

    local humanoid = getHumanoid()
    local root = getRoot()

    if humanoid then
        if originalWalkSpeed ~= nil then
            pcall(function()
                humanoid.WalkSpeed =
                    originalWalkSpeed
            end)
        end

        pcall(function()
            humanoid.PlatformStand = false
        end)
    end

    if root then
        pcall(function()
            root.AssemblyLinearVelocity =
                Vector3.zero

            root.AssemblyAngularVelocity =
                Vector3.zero
        end)
    end

    for part, oldValue in pairs(originalCollisions) do
        if part and part.Parent then
            pcall(function()
                part.CanCollide = oldValue
            end)
        end
    end

    for _, conn in ipairs(connections) do
        pcall(function()
            conn:Disconnect()
        end)
    end

    if gui then
        gui:Destroy()
    end
end

addConnection(
    closeBtn.MouseButton1Click:Connect(shutdown)
)

print("[CYBER PANEL V8 RU] Loaded")
print("[CYBER PANEL V8 RU] All functions OFF")
