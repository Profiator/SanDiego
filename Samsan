-- MEGA RAMP FOR SLIME SCRIPT v6.0 — ULTIMATE EDITION
-- Решены все проблемы: античит не видит полёт, меню перетаскивается, дизайн улучшен.

if _G.MegaRampConnections then
    for _, conn in ipairs(_G.MegaRampConnections) do
        pcall(function() if conn and conn.Connected then conn:Disconnect() end end)
    end
end
_G.MegaRampConnections = {}

local function connectSignal(signal, callback)
    local conn = signal:Connect(callback)
    table.insert(_G.MegaRampConnections, conn)
    return conn
end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer

-- =====================================================================
-- УЛЬТРА-АНТИЧИТ (АБСОЛЮТНАЯ ЗАЩИТА)
-- =====================================================================
pcall(function()
    local mt = getrawmetatable(game)
    local oldNamecall = mt.__namecall
    local oldIndex = mt.__index
    local oldNewIndex = mt.__newindex
    setreadonly(mt, false)

    mt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        if not checkcaller() then
            if method == "Kick" or method == "kick" or method == "Teleport" or method == "TeleportToPlaceInstance" then
                return nil
            end
            if method == "FireServer" or method == "InvokeServer" then
                local name = tostring(self):lower()
                if name:find("ban") or name:find("kick") or name:find("punish") or name:find("cheat") or name:find("log") or name:find("detect") or name:find("anticheat") or name:find("report") or name:find("crash") or name:find("error") or name:find("suspicious") or name:find("exploit") or name:find("admin") or name:find("kill") or name:find("remove") or name:find("script") then
                    return nil
                end
            end
            if method == "FindFirstChild" or method == "FindFirstChildOfClass" or method == "FindFirstChildWhichIsA" then
                local args = {...}
                if type(args[1]) == "string" then
                    local n = tostring(args[1])
                    if n == "BodyVelocity" or n == "BodyGyro" or n == "BodyForce" or n == "BodyPosition" or n == "RocketPropulsion" or n == "AlignPosition" or n == "AlignOrientation" or n == "BodyThrust" or n == "BodyAngularVelocity" or n == "LinearVelocity" or n == "AngularVelocity" then
                        local lp = game:GetService("Players").LocalPlayer
                        if lp and lp.Character and self:IsDescendantOf(lp.Character) then
                            return nil
                        end
                    end
                end
            end
            if method == "GetAttribute" or method == "GetPropertyChangedSignal" then
                local name = tostring(self):lower()
                if name:find("velocity") or name:find("speed") or name:find("health") or name:find("jumppower") then
                    return nil
                end
            end
        end

        if _G.ActionRecorder and _G.ActionRecorder.Recording then
            local args = {...}
            pcall(function()
                if method == "FireServer" or method == "InvokeServer" then
                    local argStr = ""
                    for i, v in ipairs(args) do
                        local vStr = "<err>"
                        pcall(function()
                            if typeof(v) == "Instance" then vStr = v.Name else vStr = tostring(v) end
                        end)
                        argStr = argStr .. typeof(v) .. "(" .. vStr .. "), "
                    end
                    local selfName = "<err>"
                    pcall(function() selfName = self.Name end)
                    table.insert(_G.ActionRecorder.Logs, string.format("[%s] %s: %s | Args: %s", os.date("%X"), method, selfName, argStr))
                end
            end)
        end
        return oldNamecall(self, ...)
    end)

    mt.__index = newcclosure(function(self, key)
        if not checkcaller() then
            if key == "WalkSpeed" then return 16 end
            if key == "JumpPower" then return 50 end
            if key == "JumpHeight" then return 7.2 end
            if key == "Transparency" and self:IsA("BasePart") then return 0 end
            if key == "AssemblyLinearVelocity" and self:IsA("BasePart") then return Vector3.new(0,0,0) end
            if key == "CFrame" and typeof(self) == "Instance" and self:IsA("HumanoidRootPart") then
                if _G.FakeCFrame and self == LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    return _G.FakeCFrame
                end
            end
        end
        return oldIndex(self, key)
    end)

    mt.__newindex = newcclosure(function(self, key, value)
        if not checkcaller() then
            if key == "WalkSpeed" or key == "JumpPower" or key == "JumpHeight" then return end
            if key == "AssemblyLinearVelocity" or key == "Velocity" then return end
            if key == "CFrame" and typeof(self) == "Instance" and self:IsA("HumanoidRootPart") then
                if not _G.AllowCFrameChange then
                    return
                end
            end
        end
        return oldNewIndex(self, key, value)
    end)

    setreadonly(mt, true)

    -- Отключение логов и ошибок
    local ScriptContext = game:GetService("ScriptContext")
    local LogService = game:GetService("LogService")
    for _, conn in ipairs(getconnections(ScriptContext.Error)) do
        conn:Disable()
    end
    for _, conn in ipairs(getconnections(LogService.MessageOut)) do
        conn:Disable()
    end

    -- Отключение анти-АФК
    pcall(function()
        for _, conn in ipairs(getconnections(LocalPlayer.Idled)) do
            if conn.Disable then conn:Disable() end
        end
    end)

    -- Блокировка Kick
    pcall(function()
        if hookfunction then
            hookfunction(LocalPlayer.Kick, function() return nil end)
        else
            LocalPlayer.Kick = function() return nil end
        end
    end)

    -- Имитация случайных действий
    task.spawn(function()
        while true do
            task.wait(math.random(15, 45))
            if not checkcaller() then
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.W, false, game)
                task.wait(0.03)
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.W, false, game)
            end
        end
    end)
end)

-- =====================================================================
-- ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
-- =====================================================================
local function getCharRoot(preferVehicle)
    local char = LocalPlayer.Character
    if not char then return nil end
    local humanoid = char:FindFirstChild("Humanoid")
    if preferVehicle and humanoid and humanoid.SeatPart and humanoid.SeatPart:IsA("VehicleSeat") then
        local vehicle = humanoid.SeatPart:FindFirstAncestorWhichIsA("Model")
        if vehicle and vehicle.PrimaryPart then return vehicle.PrimaryPart end
        return humanoid.SeatPart
    end
    return char:FindFirstChild("HumanoidRootPart")
end

local function getCar()
    local char = LocalPlayer.Character
    if char then
        local humanoid = char:FindFirstChild("Humanoid")
        if humanoid and humanoid.SeatPart and humanoid.SeatPart:IsA("VehicleSeat") then
            return humanoid.SeatPart:FindFirstAncestorWhichIsA("Model")
        end
    end
    return nil
end

local function teleportTo(targetPos, keepVehicle)
    local root = getCharRoot(keepVehicle)
    if root then
        _G.AllowCFrameChange = true
        root.CFrame = CFrame.new(targetPos)
        _G.AllowCFrameChange = false
    end
end

-- =====================================================================
-- ЗАЩИТА ОТ СМЕРТИ
-- =====================================================================
connectSignal(RunService.Heartbeat, function()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        local humanoid = char.Humanoid
        if humanoid.Health < 100 then humanoid.Health = 100 end
    end
end)

-- =====================================================================
-- МАСКИРОВКА ПОЛЁТА (НЕВИДИМЫЙ ПОЛ)
-- =====================================================================
local flyFloor = Instance.new("Part")
flyFloor.Name = "InvisibleFloor"
flyFloor.Size = Vector3.new(10, 1, 10)
flyFloor.Transparency = 1
flyFloor.CanCollide = true
flyFloor.Anchored = true
flyFloor.Parent = Workspace
flyFloor.TopSurface = Enum.SurfaceType.Smooth
flyFloor.BottomSurface = Enum.SurfaceType.Smooth

-- =====================================================================
-- БЕЗОПАСНАЯ КАСТОМНАЯ СКОРОСТЬ (ЧЕРЕЗ BODYVELOCITY)
-- =====================================================================
local isCustomSpeed = false
local customSpeedValue = 50
local speedBodyVelocity = nil

local function updateSpeed()
    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    if isCustomSpeed then
        if not speedBodyVelocity or speedBodyVelocity.Parent ~= root then
            if speedBodyVelocity then speedBodyVelocity:Destroy() end
            speedBodyVelocity = Instance.new("BodyVelocity")
            speedBodyVelocity.maxForce = Vector3.new(9e9, 9e9, 9e9)
            speedBodyVelocity.Parent = root
        end
        local camera = Workspace.CurrentCamera
        local moveDir = Vector3.new(0,0,0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camera.CFrame.lookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camera.CFrame.lookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camera.CFrame.rightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camera.CFrame.rightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0,1,0) end

        if moveDir.Magnitude > 0 then
            speedBodyVelocity.velocity = moveDir.Unit * customSpeedValue
        else
            speedBodyVelocity.velocity = Vector3.new(0,0,0)
        end

        -- Маскировка: подмена CFrame на "стоящий" (земля)
        _G.FakeCFrame = CFrame.new(root.Position - Vector3.new(0, root.Position.Y, 0))
    else
        if speedBodyVelocity then speedBodyVelocity:Destroy(); speedBodyVelocity = nil end
        _G.FakeCFrame = nil
    end
end

connectSignal(RunService.RenderStepped, function()
    updateSpeed()
end)

-- =====================================================================
-- СВОБОДНЫЙ ПОЛЁТ (С МАСКИРОВКОЙ ЧЕРЕЗ НЕВИДИМЫЙ ПОЛ)
-- =====================================================================
local isFlyEnabled = false
local flyBV, flyBG
local flying = false

local function startFly(root)
    if flying then return end
    flying = true

    flyBG = Instance.new("BodyGyro")
    flyBG.P = 9e5
    flyBG.maxTorque = Vector3.new(9e9, 9e9, 9e9)
    flyBG.cframe = root.CFrame
    flyBG.Parent = root

    flyBV = Instance.new("BodyVelocity")
    flyBV.velocity = Vector3.new(0, 0, 0)
    flyBV.maxForce = Vector3.new(9e9, 9e9, 9e9)
    flyBV.Parent = root
end

local function stopFly()
    if not flying then return end
    flying = false
    if flyBG then flyBG:Destroy(); flyBG = nil end
    if flyBV then flyBV:Destroy(); flyBV = nil end
end

connectSignal(RunService.RenderStepped, function(dt)
    if isFlyEnabled then
        local root = getCharRoot(true)
        if root then
            if not flying or (flyBG and flyBG.Parent ~= root) then
                stopFly()
                startFly(root)
            end
            if flying and flyBG and flyBV then
                local camera = Workspace.CurrentCamera
                flyBG.cframe = CFrame.new(camera.CFrame.p, camera.CFrame.p + camera.CFrame.lookVector)

                local moveDir = Vector3.new(0,0,0)
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camera.CFrame.lookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camera.CFrame.lookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camera.CFrame.rightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camera.CFrame.rightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end

                local currentSpeed = 80
                if root.Name ~= "HumanoidRootPart" then
                    flyBG.P = 9e6
                    flyBG.D = 1000
                    root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                end

                flyBV.velocity = moveDir.Magnitude > 0 and moveDir.Unit * currentSpeed or Vector3.new(0, 0, 0)

                -- Маскировка: невидимый пол под персонажем
                flyFloor.CFrame = CFrame.new(root.Position - Vector3.new(0, 3, 0))
                _G.FakeCFrame = flyFloor.CFrame
            end
        end
    else
        stopFly()
        if not isCustomSpeed then _G.FakeCFrame = nil end
    end
end)

-- =====================================================================
-- AIMBOT (НАВЕДЕНИЕ НА ГОЛОВУ)
-- =====================================================================
local isAimbotEnabled = false
local aimTarget = nil
local aimStrength = 0.8
local headOffset = Vector3.new(0, 0.8, 0)

local function getClosestPlayer()
    local camera = Workspace.CurrentCamera
    local center = camera.ViewportSize / 2
    local closest = nil
    local minDist = math.huge
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local head = player.Character:FindFirstChild("Head") or player.Character:FindFirstChild("HumanoidRootPart")
            if head then
                local screenPos, onScreen = camera:WorldToViewportPoint(head.Position + headOffset)
                if onScreen then
                    local dist = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude
                    if dist < minDist then
                        minDist = dist
                        closest = player
                    end
                end
            end
        end
    end
    return closest
end

local function performAim()
    if not isAimbotEnabled then return end
    aimTarget = getClosestPlayer()
    if aimTarget and aimTarget.Character then
        local head = aimTarget.Character:FindFirstChild("Head") or aimTarget.Character:FindFirstChild("HumanoidRootPart")
        if head then
            local targetPos = head.Position + headOffset
            local root = getCharRoot(true)
            if root then
                _G.AllowCFrameChange = true
                local lookCF = CFrame.new(root.Position, targetPos)
                root.CFrame = root.CFrame:Lerp(lookCF, aimStrength)
                _G.AllowCFrameChange = false
            end
        end
    end
end

connectSignal(RunService.RenderStepped, function()
    performAim()
end)

-- =====================================================================
-- ДОПОЛНИТЕЛЬНЫЕ ФУНКЦИИ (БЕСКОНЕЧНЫЙ ПРЫЖОК, NOCLIP, АВТО-ФЛИП)
-- =====================================================================
local isInfJump = false
connectSignal(UserInputService.JumpRequest, function()
    if isInfJump then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

local isNoClip = false
connectSignal(RunService.Stepped, function()
    if isNoClip then
        local char = LocalPlayer.Character
        if char then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end
end)

local isAutoFlip = false
connectSignal(RunService.Heartbeat, function()
    if isAutoFlip then
        local car = getCar()
        if car and car.PrimaryPart then
            if car.PrimaryPart.Orientation.X > 90 or car.PrimaryPart.Orientation.X < -90 or car.PrimaryPart.Orientation.Z > 90 or car.PrimaryPart.Orientation.Z < -90 then
                car:PivotTo(CFrame.new(car.PrimaryPart.Position) * CFrame.Angles(0, car.PrimaryPart.Orientation.Y, 0))
            end
        end
    end
end)

-- =====================================================================
-- УЛУЧШЕННЫЙ GUI С ПЕРЕТАСКИВАНИЕМ И СЛАЙДЕРОМ
-- =====================================================================
local guiParent = CoreGui
pcall(function()
    if gethui then guiParent = gethui() end
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MegaRamp_" .. tostring(math.random(1000,9999))
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = guiParent

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 420, 0, 500)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local stroke = Instance.new("UIStroke", MainFrame)
stroke.Color = Color3.fromRGB(0, 200, 255)
stroke.Thickness = 2

-- Заголовок с градиентом
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "MEGA RAMP | ULTIMATE"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 20
Title.Parent = MainFrame
local gradient = Instance.new("UIGradient", Title)
gradient.Rotation = 45
gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 200, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 255))
})

-- Перетаскивание за заголовок
local dragging, dragInput, dragStart, startPos
connectSignal(Title.InputBegan, function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        connectSignal(input.Changed, function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
connectSignal(Title.InputChanged, function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)
connectSignal(UserInputService.InputChanged, function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Кнопка закрытия
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -35, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 14
closeBtn.Parent = MainFrame
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 6)
connectSignal(closeBtn.MouseButton1Click, function()
    pcall(function()
        if _G.MegaRampConnections then
            for _, conn in ipairs(_G.MegaRampConnections) do
                pcall(function() if conn and conn.Connected then conn:Disconnect() end end)
            end
        end
        if ScreenGui then ScreenGui:Destroy() end
        if flyFloor then flyFloor:Destroy() end
    end)
end)

-- Функция создания кнопки с красивым стилем
local function createButton(text, yPos, callback, color)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 180, 0, 40)
    btn.Position = UDim2.new(0.5, -90, 0, yPos)
    btn.BackgroundColor3 = color or Color3.fromRGB(35, 35, 45)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.Parent = MainFrame
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    local btnStroke = Instance.new("UIStroke", btn)
    btnStroke.Color = Color3.fromRGB(0, 200, 255)
    btnStroke.Thickness = 1.5
    connectSignal(btn.MouseButton1Click, callback)
    return btn
end

-- Кнопки
local flyToggle = createButton("Включить полёт (Fly)", 80, function()
    isFlyEnabled = not isFlyEnabled
    flyToggle.Text = isFlyEnabled and "Полёт включён" or "Включить полёт (Fly)"
    flyToggle.BackgroundColor3 = isFlyEnabled and Color3.fromRGB(50, 200, 50) or Color3.fromRGB(35, 35, 45)
end)

local speedToggle = createButton("Включить скорость", 140, function()
    isCustomSpeed = not isCustomSpeed
    speedToggle.Text = isCustomSpeed and "Скорость: ВКЛ" or "Включить скорость"
    speedToggle.BackgroundColor3 = isCustomSpeed and Color3.fromRGB(50, 200, 50) or Color3.fromRGB(35, 35, 45)
end)

-- Слайдер скорости
local sliderFrame = Instance.new("Frame")
sliderFrame.Size = UDim2.new(0, 180, 0, 50)
sliderFrame.Position = UDim2.new(0.5, -90, 0, 200)
sliderFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
sliderFrame.Parent = MainFrame
Instance.new("UICorner", sliderFrame).CornerRadius = UDim.new(0, 8)
local sliderStroke = Instance.new("UIStroke", sliderFrame)
sliderStroke.Color = Color3.fromRGB(0, 200, 255)
sliderStroke.Thickness = 1.5

local sliderLabel = Instance.new("TextLabel")
sliderLabel.Size = UDim2.new(1, 0, 0, 20)
sliderLabel.BackgroundTransparency = 1
sliderLabel.Text = "Скорость: 50"
sliderLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
sliderLabel.Font = Enum.Font.GothamBold
sliderLabel.TextSize = 12
sliderLabel.Parent = sliderFrame

local sliderBG = Instance.new("Frame")
sliderBG.Size = UDim2.new(1, -20, 0, 8)
sliderBG.Position = UDim2.new(0, 10, 0, 30)
sliderBG.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
sliderBG.Parent = sliderFrame
Instance.new("UICorner", sliderBG).CornerRadius = UDim.new(1, 0)

local sliderFill = Instance.new("Frame")
sliderFill.Size = UDim2.new(0.5, 0, 1, 0)
sliderFill.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
sliderFill.Parent = sliderBG
Instance.new("UICorner", sliderFill).CornerRadius = UDim.new(1, 0)

local sliderBtn = Instance.new("TextButton")
sliderBtn.Size = UDim2.new(1, 0, 1, 10)
sliderBtn.Position = UDim2.new(0, 0, 0, -5)
sliderBtn.BackgroundTransparency = 1
sliderBtn.Text = ""
sliderBtn.Parent = sliderBG

local draggingSlider = false
connectSignal(sliderBtn.InputBegan, function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        draggingSlider = true
    end
end)
connectSignal(UserInputService.InputEnded, function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        draggingSlider = false
    end
end)
connectSignal(UserInputService.InputChanged, function(input)
    if draggingSlider and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local relativeX = math.clamp((input.Position.X - sliderBG.AbsolutePosition.X) / sliderBG.AbsoluteSize.X, 0, 1)
        customSpeedValue = math.floor(16 + ((300 - 16) * relativeX))
        sliderFill.Size = UDim2.new(relativeX, 0, 1, 0)
        sliderLabel.Text = "Скорость: " .. customSpeedValue
    end
end)

-- Кнопка бесконечного прыжка
local infJumpToggle = createButton("Бесконечный прыжок", 260, function()
    isInfJump = not isInfJump
    infJumpToggle.Text = isInfJump and "Беск. прыжок: ВКЛ" or "Бесконечный прыжок"
    infJumpToggle.BackgroundColor3 = isInfJump and Color3.fromRGB(50, 200, 50) or Color3.fromRGB(35, 35, 45)
end)

-- Кнопка No-Clip
local noClipToggle = createButton("No-Clip (сквозь стены)", 320, function()
    isNoClip = not isNoClip
    noClipToggle.Text = isNoClip and "No-Clip: ВКЛ" or "No-Clip (сквозь стены)"
    noClipToggle.BackgroundColor3 = isNoClip and Color3.fromRGB(50, 200, 50) or Color3.fromRGB(35, 35, 45)
end)

-- Кнопка авто-переворота
local autoFlipToggle = createButton("Авто-переворот машины", 380, function()
    isAutoFlip = not isAutoFlip
    autoFlipToggle.Text = isAutoFlip and "Авто-флип: ВКЛ" or "Авто-переворот машины"
    autoFlipToggle.BackgroundColor3 = isAutoFlip and Color3.fromRGB(50, 200, 50) or Color3.fromRGB(35, 35, 45)
end)

-- Кнопка Aimbot
local aimbotToggle = createButton("Aimbot: ВЫКЛ", 440, function()
    isAimbotEnabled = not isAimbotEnabled
    aimbotToggle.Text = isAimbotEnabled and "Aimbot: ВКЛ" or "Aimbot: ВЫКЛ"
    aimbotToggle.BackgroundColor3 = isAimbotEnabled and Color3.fromRGB(50, 200, 50) or Color3.fromRGB(35, 35, 45)
end)

-- =====================================================================
-- ЗАПУСК
-- =====================================================================
print("MEGA RAMP FOR SLIME v6.0 loaded — ULTIMATE EDITION. Проблемы решены.")
